[default]
extend-ignore-re = [
  ".*\\.svg",            # skip SVG files
  ".*\\.lock",           # skip lock files
  "docs/generated/.*",   # skip autogenerated docs if you have them
]

[files]
extend-exclude = ["CHANGELOG.md"]

[words]
Qualytics = "Allowed"
datastore = "Allowed"